using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DeckUpdator : MonoBehaviour
{
    List<int> Deck = new List<int>();
    // Start is called before the first frame update
    void Start()
    {
        Deck.Add(1);
        Deck.Add(2);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
